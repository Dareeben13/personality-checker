import type { NextPage } from "next";
import { IntroSection } from "../components/introsection";

const Home: NextPage = () => {
  return (
    <div className="">
      <IntroSection onClick={() => {}} />
    </div>
  );
};

export default Home;
